const path = require('path');

module.exports = {
  'config': path.resolve('src', 'config', 'config.js'), // Indica so Sequelize onde está localizado o arquivo de configuração;
  'models-path': path.resolve('src', 'models'), // Indica so Sequelize onde estão localizadas as models da aplicação;
  'seeders-path': path.resolve('src', 'seeders'), // Indica so Sequelize onde estão localizadas as seedders da aplicação;
  'migrations-path': path.resolve('src', 'migrations'), // Indica so Sequelize onde estão localizadas as migrations da aplicação;
};
