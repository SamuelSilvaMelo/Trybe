def is_even(number):
    "Retorna True se o número é verdadeiro, senão False."
    return number % 2 == 0


def divide(numerator, denominator):
    "Realiza a divisão de um número"
    return numerator / denominator
